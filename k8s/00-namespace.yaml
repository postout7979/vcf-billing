# [v4.11] VCF Billing Portal - Kubernetes 배포용 전용 네임스페이스.
#
# 아래 모든 매니페스트(01~07)는 이 네임스페이스를 전제로 한다. 다른 이름을 쓰고
# 싶다면 이 파일과 나머지 파일의 `namespace: vcf-billing`을 모두 동일하게 바꿔야
# 한다 (플레인 YAML 매니페스트라 Helm/Kustomize처럼 값 하나로 전체가 치환되지
# 않음 - docs/k8s-deploy.md의 "다른 네임스페이스 사용" 참고).
apiVersion: v1
kind: Namespace
metadata:
  name: vcf-billing
