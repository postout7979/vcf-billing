# [v4.0] api 컨테이너(docker/api/Dockerfile) 전용 의존성 목록.
# FastAPI 앱 자체(라우터/인증/PDF 생성 등)에 필요한 전체 패키지.
# collector 컨테이너는 이 중 일부만 필요하므로 requirements-collector.txt를 따로 둔다.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
SQLAlchemy>=2.0,<3.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-jose[cryptography]>=3.3,<4.0
python-multipart>=0.0.9,<0.1
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
reportlab>=4.2,<6.0
bcrypt>=4.0,<5.0
cryptography>=41,<44
psycopg[binary]>=3.1,<4.0
# [v4.4] "시스템 상태" 화면(API 프로세스 자체의 CPU/메모리/가동시간)용. api 컨테이너
# 자기 자신의 프로세스만 조회하며, 다른 컨테이너(collector/frontend/db)의 자원은
# 도커 소켓 마운트 없이는 조회할 수 없어 대상이 아니다 - claude/vcf-billing-portal-design.md
# "v4.4 개편" 섹션의 한계 참고.
psutil>=6.0,<7.0
