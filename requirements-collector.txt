# [v4.0] collector 컨테이너(docker/collector/Dockerfile) 전용 의존성 목록.
# FastAPI/uvicorn/reportlab/python-jose 등 API 전용 패키지는 제외해 이미지를 가볍게
# 하고, 수집 로직만 바꿨을 때 이 컨테이너만 빠르게 재빌드할 수 있도록 한다.
SQLAlchemy>=2.0,<3.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
cryptography>=41,<44
psycopg[binary]>=3.1,<4.0
# app/bootstrap_db.py(기본 admin 계정 생성)가 app/security/passwords.py를 통해 사용
bcrypt>=4.0,<5.0
